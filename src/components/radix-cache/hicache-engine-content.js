export const engineCopy={
 zh:{readyPrefix:'GPU 已就绪连续前缀',plannedReuse:'计划避免重算',plannedCompute:'计划重新计算',minimumLoad:'最小加载前缀',threshold_reject:'低于加载阈值 · 重算后缀',threshold_rejectNote:'计划加载的完整前缀小于设定阈值，示例跳过搬运，保留 GPU 前缀并重算后缀。',settings:'引擎策略',prefetchSetting:'预取等待',writeSetting:'备份时机',faultSetting:'运行条件',none:'正常运行',host_pressure:'预取主机配额耗尽',gpu_pressure:'加载配额不足',partial_read:'远端只读回半页',rank_lag:'另一 Rank 尚未完成',concurrent:'第二个请求期间，共享前缀还有一个读者',prune:'删除本地索引节点',query_pending:'远端未知 · 提交查询',allocate:'分配目标页槽',queued:'搬运已排队',inflight:'搬运进行中',victim:'选择可回收冷叶',allocation_failed:'分配失败 · 回退',drop_unbacked:'无备份可用 · 丢弃冷缓存',reader_acquire:'并发读者引用共享前缀',reader_release:'并发读者结束',chunk:'Prefill 分块 · 页尾尚未发布',consensus:'对齐多 Rank 已完成前缀',prefetch_stop:'停止预取 · 重算未完成后缀',host_pressureNote:'示例注入：预取可用主机配额为零，已有主机前缀仍可加载。跳过远端预取，用计算路径恢复后缀。',quota_reject:'加载额度不足 · 改为计算',cold_skip:'未达到热点阈值 · 暂不备份',stateTitle:'当前执行状态',pending:'已分配，未就绪',ready:'已就绪',gpuRefs:'GPU 引用',hostRefs:'主机搬运引用',candidate:'可回收冷叶',noCandidate:'无可回收页',reader:'并发读者持有 You are a helpful',tail:'请求私有页尾',unpublished:'尚未进入共享树',received:'已返回 Token',committed:'跨 Rank 对齐的完整 Token',bytes:'完成的跨层字节',unitNote:'教学池：3 层，每页 2 个示意 Token；MHA 的 K/V 向量各 32 B，MLA 单向量 32 B。GPU 4 页、主机 5 页；引用、页粒度与完成确认按模型驱动。时序为串行教学检查点，逐层重叠见下方 Layout。',pruneNote:'本地 GPU 与主机副本都已回收，因此删除本地树节点。后端可能仍有数据，但必须重新查询、读回并建立索引。',allocationNote:'目标槽位预留不等于内容可读。源页在搬运期间保持引用，完成确认后才能把目标计入就绪副本。',chunkNote:'本次 chunk 只产生页内第一个 Token。未满页由请求自己持有，不能发布到共享前缀树；下一 chunk 补齐后再插入。',consensusNote:'本例两个 Rank 取已完成连续前缀的较小值，并向下对齐到完整页。半页或落后的 Rank 都会缩短可发布前缀。',quotaNote:'本例注入加载配额为零，保留 GPU 热前缀，后缀改用预留的 Prefill 计算额度。不是在 GPU 完全无空间时强行计算。',evictNote:'从无活动引用的冷叶中选取。GPU 与主机候选独立；主机仅回收 GPU 已淘汰且不在搬运中的节点。',queryNote:'只在查询完成后揭示后端连续命中；查询前的本地匹配不读取远端目录。',pool:'KV 数据结构',mha:'MHA · K 与 V',mla:'MLA · 潜在向量',poolNote:'维度与字节为教学参数；MLA 显示完整潜在向量副本，后端写入按单份计数，不重复乘 Rank 数。',jumpLayout:'检查这页的 Layout',runtime:'本步运行时操作'},
 en:{readyPrefix:'GPU-ready continuous prefix',plannedReuse:'Planned reuse',plannedCompute:'Planned recompute',minimumLoad:'Minimum load prefix',threshold_reject:'Below load threshold · recompute suffix',threshold_rejectNote:'The complete planned load prefix is below the configured threshold. Skip the transfer, retain the GPU prefix and compute the suffix.',settings:'Engine policies',prefetchSetting:'Prefetch waiting',writeSetting:'Backup policy',faultSetting:'Runtime condition',none:'Normal',host_pressure:'No host prefetch quota',gpu_pressure:'Insufficient load quota',partial_read:'Remote read returns half a page',rank_lag:'Another rank is not ready',concurrent:'An additional reader holds the shared prefix during request two',prune:'Remove local index node',query_pending:'Remote unknown · submit query',allocate:'Reserve destination slot',queued:'Transfer queued',inflight:'Transfer in flight',victim:'Choose an evictable cold leaf',allocation_failed:'Allocation failed · fall back',drop_unbacked:'No backup available · drop cold cache',reader_acquire:'Concurrent reader locks shared prefix',reader_release:'Concurrent reader finishes',chunk:'Prefill chunk · unpublished page tail',consensus:'Agree on completed prefix across ranks',prefetch_stop:'Stop prefetch · compute incomplete suffix',host_pressureNote:'Injected condition: host prefetch quota is zero. Existing host hits can still load; skip remote prefetch and recompute the remaining suffix.',quota_reject:'Load quota rejected · recompute',cold_skip:'Below hot threshold · skip backup',stateTitle:'Current execution state',pending:'Reserved, not ready',ready:'Ready',gpuRefs:'GPU references',hostRefs:'Host transfer references',candidate:'Evictable cold leaves',noCandidate:'No evictable pages',reader:'Concurrent reader holds You are a helpful',tail:'Request-owned page tail',unpublished:'Not yet in the shared tree',received:'Returned tokens',committed:'Complete tokens agreed across ranks',bytes:'Completed transfer bytes',unitNote:'Teaching pool: 3 layers, 2 illustrative tokens per page; MHA K/V vectors are 32 B each, MLA has one 32 B vector. GPU 4 pages, host 5. References, page boundaries and acknowledgments drive the model. Checkpoints are serial; layer overlap is explored in Layout below.',pruneNote:'Both local GPU and host copies are gone, so remove the local tree node. Backend data may remain, but discovery, retrieval and index insertion must happen again.',allocationNote:'Reserving a slot does not make its contents readable. Protect the source during I/O and publish destination readiness only after acknowledgment.',chunkNote:'This chunk produces the first token of a page. The request owns this incomplete tail; the shared tree receives it only after the next chunk fills the page.',consensusNote:'Two ranks agree on the minimum completed continuous prefix, rounded down to full pages. Partial reads or a lagging rank shorten the publishable prefix.',quotaNote:'Injected load quota is zero. Keep the GPU prefix and use reserved prefill compute capacity for the suffix. This does not compute through a completely exhausted GPU.',evictNote:'Choose cold leaves without active references. GPU and host candidates differ; host eviction requires an evicted GPU copy and no active host transfer.',queryNote:'Only the query acknowledgment reveals backend prefix hits. Local matching never consults the remote directory.',pool:'KV structure',mha:'MHA · K and V',mla:'MLA · latent vector',poolNote:'Dimensions and bytes are teaching parameters. MLA shows a full latent-vector copy; backend writes count one copy, not a duplicate per rank.',jumpLayout:'Inspect this page’s layout',runtime:'Runtime operation'}
};
export const engineCode={
 match:'local = tree.match_prefix(page_aligned(tokens))\nprotect(local.device_ancestor)\nremote_status = UNKNOWN',
 query_pending:'hash_chain = hash(prefix_key, suffix_pages)\nbackend.query_async(hash_chain)',
 query:'remote_prefix = await query_ack\nplan_prefetch(remote_prefix, host_quota)',
 allocate:'protect_source()\ndst = pool.reserve(page_size)\ndst.ready = false',
 queued:'transfer_queue.push(src, dst)\nretain_source_reference()',
 inflight:'launch_copy(src, dst, stream)\nwait_for_completion_event()',
 prune:'assert node.device_value is None\nassert node.host_refs == 0\nparent.children.remove(node.key)\nupdate_evictable_parent(parent)',
 victim:'heap = unreferenced_leaf_candidates(tier)\nvictim = heap.pop_coldest()\ncheck_backup_before_device_release()',
 chunk:'request.private_tail.append(new_kv)\nif not page_aligned(tail):\n    keep_request_owned(tail)',
 compute:'finish_page()\nshared_indices = tree.insert(full_pages)\nrequest.remap_to_shared_indices(shared_indices)',
 consensus:'prefix = all_reduce(MIN, completed_tokens)\npublishable = floor_to_page(prefix)',
 load:'await copy_completion\ndevice.ready = true\nprotect_device_prefix()',
 prefetch:'await storage_read_completion\nhost.ready = true\ntree.insert(restored_prefix)',
 backup:'await D2H_completion\nhost.ready = true\nrelease_transfer_reference()',
 persist:'await backend_write_ack\nrelease_host_transfer_reference()',
 release:'decrement_request_refs_along_ancestors()\nretain_reusable_cache_pages()',
 quota_reject:'if load_size > load_quota:\n    keep_device_prefix()\n    schedule_prefill_for_suffix()',
};


