export const technicalCopy={
 zh:{windowDecodeBoundary:'这是正常 Decode 的增量窗口更新，不是每生成一个 Token 就重放尾部。有限重放发生在 Prefill 准备或相应的缓存缺失恢复时。',windowDecodeWhy:'生成当前 Token 时不做尾段重放：每层只计算本步的局部 KV，并更新自己的滑动窗口。',windowDecodeInput:'L21 接收当前 Token 的 H20；后续层接收前一层算出的当前 hidden states。已有窗口记录仍保留。',windowDecodeOperation:'各层从当前输入生成一条新的 SWA KV，追加到本层窗口；窗口超过 128 个位置时移出最旧的位置。',windowDecodeOutput:'本步新增的局部 KV 与保留的窗口历史共同供本层查询。图中只画新增记录，不代表窗口仅有一个位置。',decodeMatrices:'Decode 矩阵只展示本步新增的一行；历史全局 KV 和保留的局部窗口继续存在。',title:'技术核心：Decoder 的全局 KV 从哪里来？',hint:'先对照普通逐层路径，再点选下方整块矩阵：右侧展开它的输入、变换和输出。',positions:'位置',baseline:'普通逐层构建 KV',baselineScope:'结构对照，非 V4 性能基准',firstHalf:'前 20 层',secondHalf:'后 20 层',baselineEnd:'每层等自己的 hidden states，才能建立该层 KV',encoder:'① Encoder：处理输入，得到第 20 层输出',projection:'② 全局 KV：直接投影并共享',window:'③ 局部 KV：仍需逐层计算',generation:'④ 生成：本层 Query 查询两类历史',projectionShort:'L21 建立一份，L21–L40 共享',indexOutput:'再投影得到索引 K',allRows:'全部输入位置 → 全局历史',tailRows:'只取尾部 → 局部状态',currentRow:'当前位置 → 局部更新',skip:'其余位置不进入 Decoder：',noSkip:'当前生成位置被跳过：',globalForAll:'一份全局历史，供所有 Decoder 层读取',localForEach:'每层自己的窗口，只供本层使用',ready:'两类状态备齐，支持接下来的生成',decode:'当前 Token 顺序经过 L1–L40',queryOwn:'共享 KV ≠ 共享 Query、Attention 输出或整层计算',legend:'矩阵保留位置与维度，省略中间行/通道；条形长度表示位置数。点选只改变检查对象，不改变模型执行。',selected:'当前检查对象',input:'输入是什么',operation:'做什么变换',output:'输出交给谁',boundary:'容易混淆的地方',account:'对应上方收益的处理位置账本',accountScope:'Token×层，仅计处理位置；投影、不同算子及 GPU 效率另计，不是实际延迟。',
 baselineWhy:'普通路径的 KV 依赖该层输入，所以不能只算前半段就拿到后半段的历史。',baselineInput:'X_l 表示第 l 层输入的 hidden states；n 是此次处理的位置数，d 是隐藏维度。它来自前一层的完整计算。',baselineOperation:'在本层生成 KV，并执行 Attention 与前馈网络，得到下一层的输入。为了给后续生成准备历史，需要把提示词逐层送下去。',baselineOutput:'后 20 层分别生成自己的 KV。这个依赖顺序是对照的重点，不代表所有普通模型都有同样的缓存格式。',baselineBoundary:'这里固定 40 层比较依赖结构；不是把所有传统架构等同于 V4，也不是精确 FLOPs 对比。',
 encoderWhy:'先把整个输入编码到足够深的表示，再让后半段直接使用这份全局信息。',encoderInput:'输入为本次处理位置的 hidden states。Prefill 是整段未命中输入；Decode 只有当前一个位置。',encoderOperation:'L1–L20 顺序执行，产生第 20 层输出 H20。它保留各位置的表示，并没有把整段提示词压成一个向量。',encoderOutput:'H20 同时送往两条路径：所有位置用于投影全局 KV；尾部位置继续经过 Decoder，构建各层局部窗口。',encoderBoundary:'这是因果 Encoder，每个位置只能看到允许的历史，不是双向文本编码器，也不是视觉 Encoder。',
 projectionWhy:'全局 KV 不再等待各 Decoder 层的 hidden states，因此全文不必跑完后 20 层。',projectionInput:'取 H20 的全部位置。图中 5120 是隐藏维度，512 是主 KV 的通道数；当前 Decoder 的全局路径压缩率为 1:1。',projectionOperation:'先把 H20 投影成主 KV C，再从 C 投影出索引 K。CED 改变的是 KV 的输入来源；CSA2 进一步让 L21 的 Full 层生成一份，后续层引用它。',projectionOutput:'C 与索引 K 作为 L21–L40 共用的全局缓存。Reindex 层可以用自己的 indexer Query 重新选位置；Reuse 层沿用选择，各层再用自己的主 Query 做 Attention。',projectionBoundary:'CED 本身允许 Decoder 层使用各自投影；“20 层只存一份”来自本模型叠加的 CSA2 配置。投影仍有计算成本。公式省略归一化、位置编码与量化，FP4 改变存储字节，不改变记录数。',
 windowWhy:'局部窗口没有改为共享 H20，因此仍需把尾部位置逐层送过 Decoder。',windowInput:'截取 H20 的最后至多 128 个位置作为 L21 的输入。此后的每一层接收前一层算出的 hidden states，而不是所有层直接对 H20 建窗口。',windowOperation:'每个 Decoder 层执行自己的计算，并从本层输入生成本层 SWA KV。重放段内的局部 Attention 截断在该段起点；可用的全局历史仍保留。',windowOutput:'生成 L21–L40 各自的局部 KV，供后续生成使用。局部窗口不是第二份全局缓存，也不是把全局历史裁剪为 128 个位置。',windowBoundary:'这条路径是 Decoder Bounded Replay。另一个 Encoder Bounded Replay 处理的是“全局缓存命中但 Encoder 局部窗口丢失”；两者触发条件不同。',
 generationWhy:'预先建立历史省的是 Prefill；生成新内容仍需要完整的计算深度。',generationInput:'每层接收当前 hidden states、自身局部窗口，以及共享全局缓存中选出的记录。索引器负责挑选全局记录，主 Query 负责真正的 Attention。',generationOperation:'每层生成自己的 Query，将选中的全局记录与本层局部窗口一起用于 Attention，再执行后续层内计算。图中 Attention 式只展示缓存依赖，省略头展开和输出投影。',generationOutput:'各层产生不同输出，最后得到生成分布。相同 KV 不会让不同层变成相同计算，也不会把 Decoder 从生成路径中删掉。',generationBoundary:'Decode 的新位置同时更新需要更新的全局缓存和各层局部状态；图中矩阵只画本步新增记录，已有历史并未消失。',prefillBoundary:'尾部重放是近似恢复；报告称质量影响很小，但不保证与完整 Decoder 前向数值一致。',decodeBoundary:'当前一步经过全部 40 层，无“只运行前 20 层”的生成捷径。'},
 en:{windowDecodeBoundary:'This is ordinary incremental decode, not a tail replay for every generated token. Bounded replay belongs to prefill preparation or the relevant cache-miss recovery.',windowDecodeWhy:'Decode does not replay the tail: each layer computes new local KV for the current token and updates its own window.',windowDecodeInput:'L21 receives current-token H20; later layers receive the preceding layer’s current hidden states. Previously retained window records remain.',windowDecodeOperation:'Create and append one new SWA KV record per layer. If a window exceeds 128 positions, remove its oldest position.',windowDecodeOutput:'The new record joins retained local history for this layer’s query. A single displayed row means a new record, not an empty prior window.',decodeMatrices:'Decode matrices show only the newly created row. Prior global KV and retained local windows remain available.',title:'Core mechanism: where does decoder global KV come from?',hint:'Compare the layer-by-layer dependency, then select a matrix block to inspect its input, operation and output.',positions:'positions',baseline:'Conventional layer-by-layer KV construction',baselineScope:'Structural comparison, not a V4 benchmark',firstHalf:'First 20 layers',secondHalf:'Last 20 layers',baselineEnd:'Each layer needs its own hidden states before it can construct KV',encoder:'① Encoder: produce layer-20 representations',projection:'② Global KV: project directly and share',window:'③ Local KV: still computed in each layer',generation:'④ Generation: each query reads both histories',projectionShort:'L21 creates one copy, shared by L21–L40',indexOutput:'Project indexer K from main KV',allRows:'All input positions → global history',tailRows:'Tail positions only → local state',currentRow:'Current position → local update',skip:'Positions not sent through the decoder:',noSkip:'Current generation positions skipped:',globalForAll:'One global history read by every decoder layer',localForEach:'Each local window serves its own layer',ready:'Both state types support subsequent generation',decode:'The current token runs through L1–L40',queryOwn:'Shared KV ≠ shared queries, attention outputs or layer computation',legend:'Matrices retain positions and dimensions with omitted rows/channels; bar lengths encode position counts. Selection inspects the model without changing execution.',selected:'Inspecting',input:'Input',operation:'Operation',output:'Output and consumer',boundary:'Key distinction',account:'Position accounting behind the benefit above',accountScope:'Token-layers only; projection, operator differences and GPU efficiency are separate. Not measured latency.',
 baselineWhy:'Conventional KV depends on each layer’s input, so the first half cannot alone supply the second half’s history.',baselineInput:'X_l is the hidden-state input to layer l; n is the number of processed positions and d the hidden dimension. It comes from the preceding layer’s computation.',baselineOperation:'Construct KV in each layer and execute attention and feed-forward computation to produce the next layer’s input. Building history requires passing the prompt down the stack.',baselineOutput:'Each of the last 20 layers obtains its own KV. This illustrates a dependency, not a universal cache representation.',baselineBoundary:'This holds depth at 40 for a structural comparison. It neither equates all conventional architectures with V4 nor measures exact FLOPs.',
 encoderWhy:'Build a deep representation of the entire input that can directly supply global information to the second half.',encoderInput:'Hidden states for the positions processed now: an uncached prompt in prefill, or one current position in decode.',encoderOperation:'Run L1–L20 sequentially to obtain H20. Position-wise representations remain; this is not a single-vector summary of the prompt.',encoderOutput:'All H20 positions supply global KV projection; tail positions also enter the decoder to build layer-local windows.',encoderBoundary:'The encoder is causal, seeing only permitted history. It is neither a bidirectional text encoder nor the vision encoder.',
 projectionWhy:'Global KV no longer waits for each decoder layer’s hidden states, so the entire prompt need not run through the last 20 layers.',projectionInput:'Take all positions in H20. The diagram shows hidden dimension 5120 and main KV dimension 512. This decoder global path has a 1:1 position ratio.',projectionOperation:'Project H20 into main KV C, then project indexer K from C. CED changes the source of KV; CSA2 additionally makes the Full layer L21 create one copy reused by later layers.',projectionOutput:'C and indexer K serve L21–L40. Reindex layers may select new positions with their own indexer queries; Reuse layers retain a selection. Each layer still performs attention with its own main query.',projectionBoundary:'CED alone permits layer-dependent decoder projections. The single shared copy comes from this model’s CSA2 configuration. Projection still costs compute. Normalization, positional encoding and quantization are omitted; FP4 changes bytes, not record count.',
 windowWhy:'Local windows do not share H20 as their direct source, so the tail must still pass through the decoder layers.',windowInput:'The last at most 128 H20 positions enter L21. Each subsequent layer receives the previous layer’s result, rather than constructing every window directly from H20.',windowOperation:'Each decoder layer computes its own SWA KV from its own input. Local attention is truncated at the replay segment’s start; available global history is retained.',windowOutput:'Layer-specific local KV for L21–L40 support generation. These windows neither duplicate global history nor truncate it to 128 positions.',windowBoundary:'This is Decoder Bounded Replay. Encoder Bounded Replay instead handles a global-cache hit with missing encoder local state; their triggers differ.',
 generationWhy:'Prebuilding history saves prefill work; generating content still needs the full computational depth.',generationInput:'Each layer receives current hidden states, its local window and selected entries from shared global KV. The indexer selects entries; the main query performs attention.',generationOperation:'Build a layer-specific query and attend jointly to selected global records and the local window, then execute the remaining layer computation. The formula shows cache dependencies, omitting head expansion and output projection.',generationOutput:'Layers produce distinct outputs, leading to the final generation distribution. Shared KV does not make the layers identical or remove the decoder from generation.',generationBoundary:'New decode positions update required global caches and local states. Matrices show only newly generated records; existing history remains.',prefillBoundary:'Tail replay is approximate. The report describes a small quality impact, not numerical equivalence to full decoder processing.',decodeBoundary:'The current position runs through all 40 layers. Generation does not take a first-20-layers-only shortcut.'}
};
